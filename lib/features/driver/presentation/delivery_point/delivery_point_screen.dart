import 'package:flutter/material.dart';

class DeliveryPointScreen extends StatelessWidget {
  const DeliveryPointScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Delivery Point')),
      body: const Center(child: Text('Delivery Point Screen')),
    );
  }
}
